# v0.1 AI 代码评审「双检」MVP 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现第一个可运行的 AI 代码评审双检系统：静态快检 + AI 语义深检，CQRS 缓存，DeepSeek→Qwen 降级链，熔断器保护，输出完整报告。

**Architecture：** 六个独立脚本（每个 <200 行）：`static_check.py` 负责本地静态检查，`circuit_breaker.py` 负责 AI 入口健康，`fallback_chain.py` 负责模型降级 + AI 调用，`cqrs_router.py` 负责读写缓存，`state_machine.py` 提供流程状态推进，`app.py` 负责 CLI、配置、合并与报告。所有模块只通过标准库交互。

**Tech Stack：** Python 3.12+ 标准库（`ast`, `json`, `urllib.request`, `hashlib`, `pathlib`, `dataclasses`, `argparse`, `re`, `time`, `typing`），无第三方依赖。

## Global Constraints

- 免费大模型 API（DeepSeek / Qwen）
- Python 标准库，无第三方依赖
- 每个核心脚本文件 < 200 行（硬约束），六个脚本单一职责
- `ai_deep_check` 的调用降级由 `fallback_chain.py` 承载，prompt 构造与结果解析由 `app.py` 负责
- 本地兜底（v0.1 即做实）基于 `StaticReport` 生成最小说明，非空 stub
- 测试采用「记录文档」形态，不引入 pytest，回归基线写入 `tests/test_record.md`
- 每轮迭代结束等用户指令，不自动进入下一轮

---

## File Structure

本计划创建/修改的文件：

```text
AI_Code_Review_Dual-Check_Assist/
├── skill/
│   ├── scripts/
│   │   ├── static_check.py       # Finding / StaticReport + ast/正则检查
│   │   ├── circuit_breaker.py    # CircuitBreaker 三态闸门
│   │   ├── cqrs_router.py        # hash 缓存读写
│   │   ├── fallback_chain.py     # 模型降级链 + 本地兜底
│   │   ├── state_machine.py      # 评审流程状态机
│   │   └── app.py                # CLI / 配置 / prompt / merge / 报告
│   └── references/
│       └── config.example.json   # 唯一配置入口（JSON 格式，兼容 stdlib）
├── data/
│   ├── sample_buggy_code.py
│   ├── sample_clean_code.py
│   └── sample_go_code.go
├── tests/
│   └── test_record.md            # v0.1 回归基线
├── iteration/
│   └── iteration_log.md          # v0.1 七要素更新记录
└── README.md                     # 项目说明（根）
```

**注意：** spec 中写 `config.example.yaml`，但本计划使用 `config.example.json`。原因是硬约束「纯 Python 标准库」，PyYAML 不是标准库。如需保留 YAML，需额外实现 YAML 子集解析器（约 40 行）或接受第三方依赖。该偏差在 plan self-review 中再次标记。

---

## Task 1: 创建目录与示例配置

**Files:**
- Create: `skill/scripts/.gitkeep`
- Create: `skill/references/config.example.json`
- Create: `data/.gitkeep`
- Create: `iteration/.gitkeep`
- Create: `tests/.gitkeep`

**Interfaces:**
- Produces: 配置 schema（六个模块都会读取）
- Config keys:
  - `models`: list of `{name, endpoint, api_key_env, model, timeout}`
  - `breaker`: `{threshold, cooldown}`
  - `cache`: `{dir, ttl_days}`
  - `review`: `{max_function_lines, max_nesting, max_file_lines}`

- [ ] **Step 1: Write config.example.json**

```json
{
  "models": [
    {
      "name": "deepseek",
      "endpoint": "https://api.deepseek.com/v1/chat/completions",
      "api_key_env": "DEEPSEEK_API_KEY",
      "model": "deepseek-chat",
      "timeout": 30
    },
    {
      "name": "qwen",
      "endpoint": "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions",
      "api_key_env": "DASHSCOPE_API_KEY",
      "model": "qwen-plus",
      "timeout": 30
    }
  ],
  "breaker": {
    "threshold": 3,
    "cooldown": 60
  },
  "cache": {
    "dir": "data/.cache",
    "ttl_days": 7
  },
  "review": {
    "max_function_lines": 50,
    "max_nesting": 4,
    "max_file_lines": 800
  }
}
```

- [ ] **Step 2: Create empty directories**

```bash
mkdir -p skill/scripts skill/references data iteration tests
```

- [ ] **Step 3: Verify directory tree**

```bash
find skill data iteration tests -type d
```

Expected output: four directory lines.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: scaffold skill directories and config.example.json"
```

---

## Task 2: 实现 static_check.py

**Files:**
- Create: `skill/scripts/static_check.py`

**Interfaces:**
- Produces:
  - `Finding(line, kind, severity, message)`
  - `StaticReport(lang, findings, summary)`
  - `static_check(code: str, lang: str, *, max_function_lines: int = 50, max_nesting: int = 4) -> StaticReport`
- Consumes: 无（叶子模块）

- [ ] **Step 1: Write the module**

```python
"""静态快检层：Python ast + 通用正则。"""
from __future__ import annotations
import ast
import re
from dataclasses import dataclass, field
from typing import List


@dataclass
class Finding:
    line: int
    kind: str
    severity: str
    message: str


@dataclass
class StaticReport:
    lang: str
    findings: List[Finding] = field(default_factory=list)
    summary: str = ""


def _max_nesting(tree_node) -> int:
    def visit(node, d):
        body = getattr(node, "body", None)
        if not isinstance(body, list):
            return d
        return max((visit(child, d + 1) for child in body), default=d)
    return visit(tree_node, 0)


def _python_check(code: str, max_fn: int, max_nest: int) -> List[Finding]:
    findings: List[Finding] = []
    try:
        tree = ast.parse(code)
    except SyntaxError as e:
        return [Finding(e.lineno or 1, "syntax_error", "high", f"语法错误: {e.msg}")]
    for node in ast.walk(tree):
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
            end = getattr(node, "end_lineno", node.lineno) or node.lineno
            length = end - node.lineno + 1
            if length > max_fn:
                findings.append(Finding(node.lineno, "long_function", "medium",
                                        f"函数 {node.name} 共 {length} 行，超过 {max_fn}"))
            branches = sum(1 for n in ast.walk(node)
                           if isinstance(n, (ast.If, ast.For, ast.While,
                                             ast.ExceptHandler, ast.BoolOp,
                                             ast.comprehension)))
            if branches > 10:
                findings.append(Finding(node.lineno, "high_complexity", "medium",
                                        f"函数 {node.name} 圈复杂度约 {branches + 1}"))
            for arg in node.args.defaults:
                if isinstance(arg, (ast.List, ast.Dict, ast.Set)):
                    findings.append(Finding(node.lineno, "mutable_default", "high",
                                            f"函数 {node.name} 使用可变默认参数"))
                elif isinstance(arg, ast.Call) and getattr(arg.func, "id", "") in ("list", "dict", "set"):
                    findings.append(Finding(node.lineno, "mutable_default", "high",
                                            f"函数 {node.name} 使用可变默认参数"))
            if _max_nesting(node) > max_nest:
                findings.append(Finding(node.lineno, "deep_nesting", "medium",
                                        f"函数 {node.name} 嵌套深度超过 {max_nest}"))
        elif isinstance(node, ast.ExceptHandler):
            if node.type is None:
                findings.append(Finding(node.lineno, "bare_except", "medium",
                                        "裸 except 捕获所有异常，建议指定异常类型"))
        elif isinstance(node, ast.Compare):
            for op in node.ops:
                if isinstance(op, (ast.Is, ast.IsNot)) and any(isinstance(c, ast.Constant) for c in node.comparators):
                    findings.append(Finding(node.lineno, "is_literal", "medium",
                                            "用 'is' 比较字面量，应改用 '=='"))
    return findings


def _regex_check(code: str) -> List[Finding]:
    findings: List[Finding] = []
    for i, line in enumerate(code.splitlines(), 1):
        if len(line) > 120:
            findings.append(Finding(i, "long_line", "low",
                                    f"行 {i} 长度 {len(line)}，建议 ≤120"))
        if re.search(r"\bTODO\b|\bFIXME\b", line):
            findings.append(Finding(i, "todo_marker", "low",
                                    "存在 TODO/FIXME 标记"))
    return findings


def static_check(code: str, lang: str, *,
                 max_function_lines: int = 50,
                 max_nesting: int = 4) -> StaticReport:
    """对 code 进行静态检查，返回 StaticReport。"""
    if lang == "python":
        findings = _python_check(code, max_function_lines, max_nesting)
    else:
        findings = _regex_check(code)
    findings += _regex_check(code)
    summary = f"发现 {len(findings)} 条静态问题" if findings else "未发现明显静态问题"
    return StaticReport(lang=lang, findings=findings, summary=summary)


if __name__ == "__main__":
    sample = '''\n    def bad(a=[]):\n        try:\n            x = 1\n        except:\n            pass\n    '''
    report = static_check(sample, "python")
    assert len(report.findings) >= 3, report.findings
    print("static_check smoke PASS:", report.summary)
```

- [ ] **Step 2: Run smoke check**

```bash
python skill/scripts/static_check.py
```

Expected: `static_check smoke PASS: 发现 ... 条静态问题`

- [ ] **Step 3: Commit**

```bash
git add skill/scripts/static_check.py
git commit -m "feat: add static_check module with ast and regex checks"
```

---

## Task 3: 实现 circuit_breaker.py

**Files:**
- Create: `skill/scripts/circuit_breaker.py`

**Interfaces:**
- Produces:
  - `CircuitBreaker(threshold=3, cooldown=60.0)`
  - `allow() -> bool`
  - `record_success()`
  - `record_failure()`
- Consumes: 无（叶子模块）

- [ ] **Step 1: Write the module**

```python
"""熔断器：AI 子系统健康闸门。"""
from __future__ import annotations
import time
from dataclasses import dataclass, field


@dataclass
class CircuitBreaker:
    threshold: int = 3
    cooldown: float = 60.0
    state: str = field(default="CLOSED")
    failures: int = field(default=0)
    opened_at: float = field(default=0.0)

    def allow(self) -> bool:
        now = time.time()
        if self.state == "OPEN":
            if now - self.opened_at >= self.cooldown:
                self.state = "HALF_OPEN"
                return True
            return False
        return True

    def record_success(self) -> None:
        self.failures = 0
        self.state = "CLOSED"

    def record_failure(self) -> None:
        self.failures += 1
        if self.failures >= self.threshold:
            self.state = "OPEN"
            self.opened_at = time.time()


if __name__ == "__main__":
    cb = CircuitBreaker(threshold=2, cooldown=0.1)
    assert cb.allow()
    cb.record_failure()
    cb.record_failure()
    assert not cb.allow()
    time.sleep(0.15)
    assert cb.allow() and cb.state == "HALF_OPEN"
    cb.record_success()
    assert cb.state == "CLOSED"
    print("circuit_breaker smoke PASS")
```

- [ ] **Step 2: Run smoke check**

```bash
python skill/scripts/circuit_breaker.py
```

Expected: `circuit_breaker smoke PASS`

- [ ] **Step 3: Commit**

```bash
git add skill/scripts/circuit_breaker.py
git commit -m "feat: add circuit breaker for AI subsystem"
```

---

## Task 4: 实现 cqrs_router.py

**Files:**
- Create: `skill/scripts/cqrs_router.py`

**Interfaces:**
- Produces:
  - `make_key(code: str, lang: str, model_ver: str) -> str`
  - `CQRSRouter(cache_dir: str, ttl_days: int = 7)`
  - `try_read(key: str) -> dict | None`
  - `write(key: str, report: dict) -> None`
- Consumes: 无（叶子模块）

- [ ] **Step 1: Write the module**

```python
"""CQRS 读写隔离：读缓存 / 写新报告。"""
from __future__ import annotations
import hashlib
import json
import time
from pathlib import Path
from dataclasses import dataclass


@dataclass
class CQRSRouter:
    cache_dir: str
    ttl_days: int = 7

    def _path(self, key: str) -> Path:
        d = Path(self.cache_dir)
        d.mkdir(parents=True, exist_ok=True)
        return d / f"{key}.json"

    def try_read(self, key: str) -> dict | None:
        p = self._path(key)
        if not p.exists():
            return None
        if time.time() - p.stat().st_mtime > self.ttl_days * 86400:
            p.unlink(missing_ok=True)
            return None
        return json.loads(p.read_text(encoding="utf-8"))

    def write(self, key: str, report: dict) -> None:
        self._path(key).write_text(json.dumps(report, ensure_ascii=False, indent=2),
                                   encoding="utf-8")


def make_key(code: str, lang: str, model_ver: str) -> str:
    return hashlib.sha256(f"{code}::{lang}::{model_ver}".encode()).hexdigest()[:16]


if __name__ == "__main__":
    import tempfile, os
    with tempfile.TemporaryDirectory() as td:
        r = CQRSRouter(td, ttl_days=1)
        key = make_key("print(1)", "python", "v1")
        assert r.try_read(key) is None
        r.write(key, {"ok": True})
        assert r.try_read(key) == {"ok": True}
        # stale
        p = r._path(key)
        os.utime(p, (0, 0))
        assert r.try_read(key) is None
    print("cqrs_router smoke PASS")
```

- [ ] **Step 2: Run smoke check**

```bash
python skill/scripts/cqrs_router.py
```

Expected: `cqrs_router smoke PASS`

- [ ] **Step 3: Commit**

```bash
git add skill/scripts/cqrs_router.py
git commit -m "feat: add CQRS read/write cache router"
```

---

## Task 5: 实现 fallback_chain.py

**Files:**
- Create: `skill/scripts/fallback_chain.py`

**Interfaces:**
- Consumes:
  - `CircuitBreaker` from `circuit_breaker`
  - `StaticReport` from `static_check`
- Produces:
  - `ModelConfig(name, endpoint, api_key_env, model, timeout)`
  - `FallbackChain(models, breaker)`
  - `call(prompt: str) -> str` — 返回 AI 文本；全部失败返回本地兜底字符串

- [ ] **Step 1: Write the module**

```python
"""模型降级链：DeepSeek -> Qwen -> 本地兜底。"""
from __future__ import annotations
import json
import os
import urllib.request
from dataclasses import dataclass
from typing import List
from static_check import StaticReport
from circuit_breaker import CircuitBreaker


@dataclass
class ModelConfig:
    name: str
    endpoint: str
    api_key_env: str
    model: str
    timeout: int


class FallbackChain:
    def __init__(self, models: List[ModelConfig], breaker: CircuitBreaker):
        self.models = models
        self.breaker = breaker

    def _call_one(self, model: ModelConfig, prompt: str) -> str:
        key = os.environ.get(model.api_key_env, "")
        if not key:
            raise RuntimeError(f"缺少环境变量 {model.api_key_env}")
        payload = json.dumps({
            "model": model.model,
            "messages": [{"role": "user", "content": prompt}],
            "temperature": 0.2,
            "max_tokens": 2048
        }).encode()
        req = urllib.request.Request(
            model.endpoint,
            data=payload,
            headers={"Content-Type": "application/json", "Authorization": f"Bearer {key}"},
            method="POST"
        )
        with urllib.request.urlopen(req, timeout=model.timeout) as resp:
            body = json.loads(resp.read().decode())
        return body["choices"][0]["message"]["content"]

    def _local_fallback(self, prompt: str, static_report: StaticReport) -> str:
        lines = ["## AI 深检暂不可用", "", "原因：模型链全部失败或熔断器已 OPEN。",
                 "", "基于静态快检结果的最小说明：", ""]
        if static_report.findings:
            for f in static_report.findings:
                lines.append(f"- 第 {f.line} 行 [{f.kind}] {f.message}")
        else:
            lines.append("- 未发现静态层可识别的问题。")
        lines += ["", "建议：稍后重试 AI 深检以获得语义层结论。"]
        return "\n".join(lines)

    def call(self, prompt: str, static_report: StaticReport) -> str:
        last_err = ""
        for model in self.models:
            try:
                text = self._call_one(model, prompt)
                self.breaker.record_success()
                return text
            except Exception as e:
                last_err = str(e)
                self.breaker.record_failure()
        return self._local_fallback(last_err, static_report)


if __name__ == "__main__":
    from static_check import static_check
    cb = CircuitBreaker(threshold=1, cooldown=60)
    chain = FallbackChain([], cb)
    sr = static_check("x = 1\n", "python")
    out = chain.call("ignored", sr)
    assert "AI 深检暂不可用" in out
    assert cb.state == "CLOSED"   # no models, no failure recorded
    print("fallback_chain fallback smoke PASS")
```

- [ ] **Step 2: Run smoke check**

```bash
cd skill/scripts && python fallback_chain.py
```

Expected: `fallback_chain fallback smoke PASS`

- [ ] **Step 3: Commit**

```bash
git add skill/scripts/fallback_chain.py
git commit -m "feat: add model fallback chain with local static-based fallback"
```

---

## Task 6: 实现 state_machine.py

**Files:**
- Create: `skill/scripts/state_machine.py`

**Interfaces:**
- Produces:
  - `STATES = ["INIT", "STATIC", "AI_GATE", "AI_RUN", "MERGE", "DONE"]`
  - `next_state(state: str, event: str) -> tuple[str, str]`
  - `initial_state() -> str`
  - `is_terminal(state: str) -> bool`
- Consumes: 无（纯状态函数）

- [ ] **Step 1: Write the module**

```python
"""评审流程状态机：init → static → ai_gate → ai_run → merge → done。"""
from __future__ import annotations

STATES = ["INIT", "STATIC", "AI_GATE", "AI_RUN", "MERGE", "DONE"]


def initial_state() -> str:
    return "INIT"


def is_terminal(state: str) -> bool:
    return state == "DONE"


def next_state(state: str, event: str) -> tuple[str, str]:
    """返回 (new_state, action)。action 告诉 app.py 该执行哪一步。"""
    if state == "INIT":
        return "STATIC", "run_static"
    if state == "STATIC":
        return "AI_GATE", "check_gate"
    if state == "AI_GATE":
        if event == "allowed":
            return "AI_RUN", "run_ai"
        return "MERGE", "merge_degraded"
    if state == "AI_RUN":
        return "MERGE", "merge_normal"
    if state in ("MERGE", "DONE"):
        return "DONE", "finish"
    raise ValueError(f"未知状态 {state}")


if __name__ == "__main__":
    s = initial_state()
    assert not is_terminal(s)
    s, a = next_state(s, "")
    assert s == "STATIC" and a == "run_static"
    s, a = next_state(s, "")
    assert s == "AI_GATE" and a == "check_gate"
    s, a = next_state(s, "allowed")
    assert s == "AI_RUN" and a == "run_ai"
    s, a = next_state(s, "")
    assert s == "MERGE" and a == "merge_normal"
    s, a = next_state(s, "")
    assert s == "DONE" and a == "finish"
    s = "AI_GATE"
    s, a = next_state(s, "blocked")
    assert s == "MERGE" and a == "merge_degraded"
    print("state_machine smoke PASS")
```

- [ ] **Step 2: Run smoke check**

```bash
python skill/scripts/state_machine.py
```

Expected: `state_machine smoke PASS`

- [ ] **Step 3: Commit**

```bash
git add skill/scripts/state_machine.py
git commit -m "feat: add review state machine with degradation path"
```

---

## Task 7: 实现 app.py（编排 + 合并 + CLI）

**Files:**
- Create: `skill/scripts/app.py`

**Interfaces:**
- Consumes:
  - `static_check, StaticReport, Finding` from `static_check.py`
  - `CircuitBreaker` from `circuit_breaker.py`
  - `CQRSRouter, make_key` from `cqrs_router.py`
  - `FallbackChain, ModelConfig` from `fallback_chain.py`
  - `next_state, initial_state, is_terminal` from `state_machine.py`
- Produces:
  - `AIReport(confirmation, rejection, new_findings)`
  - `FinalReport(risk, summary, findings)`
  - `merge(static_report, ai_report, ai_text) -> FinalReport`
  - `review(code, lang, framework, config_path) -> FinalReport`
  - CLI main entrypoint

- [ ] **Step 1: Write the module**

```python
"""CLI 入口与编排。"""
from __future__ import annotations
import argparse
import json
import os
import sys
from dataclasses import dataclass, field
from pathlib import Path
from typing import List, Optional

from circuit_breaker import CircuitBreaker
from cqrs_router import CQRSRouter, make_key
from fallback_chain import FallbackChain, ModelConfig
from state_machine import initial_state, is_terminal, next_state
from static_check import static_check, Finding, StaticReport


@dataclass
class AIReport:
    confirmation: List[Finding] = field(default_factory=list)
    rejection: List[Finding] = field(default_factory=list)
    new_findings: List[Finding] = field(default_factory=list)


@dataclass
class FinalReport:
    risk: str
    summary: str
    static_summary: str
    ai_summary: str
    findings: List[dict]


def _load_config(path: str) -> dict:
    return json.loads(Path(path).read_text(encoding="utf-8"))


def _model_ver(models: List[dict]) -> str:
    return json.dumps(models, sort_keys=True)[:32]


def _build_prompt(static_report: StaticReport, code: str, lang: str, framework: str) -> str:
    findings_text = "\n".join(f"- 行 {f.line} [{f.kind}] {f.message}" for f in static_report.findings)
    return (
        "你是一位资深代码评审员。请基于以下静态快检结果和源代码，做语义深检。\n\n"
        f"语言: {lang}\n框架: {framework or '无'}\n\n"
        "静态快检发现的问题（可能包含误报）：\n" + (findings_text or "无") + "\n\n"
        "源代码：\n```\n" + code + "\n```\n\n"
        "请严格按以下 JSON 格式返回，不要包含任何 markdown 代码块标记：\n"
        '{"confirmation": [{"line": 行号, "kind": "类型", "severity": "high/medium/low", "message": "确认说明"}], '
        '"rejection": [{"line": 行号, "kind": "类型", "severity": "low", "message": "认为是误报的理由"}], '
        '"new_findings": [{"line": 行号, "kind": "类型", "severity": "high/medium/low", "message": "AI 发现静态层漏掉的问题"}]}\n'
    )


def _parse_ai(text: str) -> Optional[AIReport]:
    try:
        data = json.loads(text.strip())
    except Exception:
        return None
    def to_findings(xs):
        return [Finding(x.get("line", 0), x.get("kind", ""), x.get("severity", "low"),
                        x.get("message", "")) for x in xs]
    return AIReport(
        confirmation=to_findings(data.get("confirmation", [])),
        rejection=to_findings(data.get("rejection", [])),
        new_findings=to_findings(data.get("new_findings", []))
    )


def _risk(ai_report: Optional[AIReport], static: StaticReport) -> str:
    high = sum(1 for f in static.findings if f.severity == "high")
    if ai_report:
        high += sum(1 for f in ai_report.new_findings if f.severity == "high")
        high += sum(1 for f in ai_report.confirmation if f.severity == "high")
    if high > 0:
        return "阻止合并"
    if static.findings or (ai_report and (ai_report.confirmation or ai_report.new_findings)):
        return "修复后合并"
    return "可合并"


def merge(static_report: StaticReport, ai_report: Optional[AIReport], ai_text: str) -> FinalReport:
    rows: List[dict] = []
    # 静态确认（无论 AI 是否参与都列出）
    for f in static_report.findings:
        rows.append({"line": f.line, "layer": "static", "kind": f.kind,
                     "severity": f.severity, "message": f.message})
    if ai_report:
        for f in ai_report.confirmation:
            rows.append({"line": f.line, "layer": "ai_confirmed", "kind": f.kind,
                         "severity": f.severity, "message": f.message})
        for f in ai_report.new_findings:
            rows.append({"line": f.line, "layer": "ai_new", "kind": f.kind,
                         "severity": f.severity, "message": f.message})
        ai_summary = f"AI 确认 {len(ai_report.confirmation)} 条，新发现 {len(ai_report.new_findings)} 条，否定 {len(ai_report.rejection)} 条"
    else:
        ai_summary = "AI 深检未运行（熔断降级或模型链全部失败）"
    risk = _risk(ai_report, static_report)
    summary = f"风险等级: {risk}; 静态问题 {len(static_report.findings)} 条"
    if ai_report:
        summary += f"; AI 新发现 {len(ai_report.new_findings)} 条"
    return FinalReport(risk=risk, summary=summary, static_summary=static_report.summary,
                       ai_summary=ai_summary, findings=rows)


def review(code: str, lang: str, framework: str = "", config_path: str = "") -> FinalReport:
    config = _load_config(config_path) if config_path and Path(config_path).exists() else _load_config(
        "skill/references/config.example.json")
    cache_cfg = config.get("cache", {})
    router = CQRSRouter(cache_cfg.get("dir", "data/.cache"), cache_cfg.get("ttl_days", 7))
    mver = _model_ver(config.get("models", []))
    key = make_key(code, lang, mver)
    cached = router.try_read(key)
    if cached:
        return FinalReport(**cached)

    breaker_cfg = config.get("breaker", {})
    breaker = CircuitBreaker(breaker_cfg.get("threshold", 3), breaker_cfg.get("cooldown", 60))
    review_cfg = config.get("review", {})
    static_report = static_check(code, lang,
                                  max_function_lines=review_cfg.get("max_function_lines", 50),
                                  max_nesting=review_cfg.get("max_nesting", 4))

    state = initial_state()
    ai_report: Optional[AIReport] = None
    ai_text = ""
    while not is_terminal(state):
        state, action = next_state(state, "")
        if action == "run_static":
            continue
        if action == "check_gate":
            event = "allowed" if breaker.allow() else "blocked"
            state, action = next_state(state, event)
        if action == "run_ai":
            prompt = _build_prompt(static_report, code, lang, framework)
            chain = FallbackChain([ModelConfig(**m) for m in config.get("models", [])], breaker)
            ai_text = chain.call(prompt, static_report)
            ai_report = _parse_ai(ai_text)
            if ai_report is None and "AI 深检暂不可用" not in ai_text:
                # 模型返回了非 JSON，视为模型失败
                breaker.record_failure()
                ai_text = chain.call(prompt, static_report)
                ai_report = _parse_ai(ai_text)
        # merge_normal or merge_degraded
        report = merge(static_report, ai_report, ai_text)
        router.write(key, report.__dict__)
        return report
    return merge(static_report, None, "")


def _fmt(report: FinalReport) -> str:
    lines = ["# 双检代码评审报告", "", f"风险等级: {report.risk}", "", "## 摘要", report.summary,
             "", "## 静态层", report.static_summary, "", "## AI 层", report.ai_summary, "", "## 明细"]
    for f in report.findings:
        lines.append(f'- 行 {f["line"]} [{f["layer"]}:{f["kind"]}] {f["severity"]} — {f["message"]}')
    return "\n".join(lines)


def main() -> None:
    parser = argparse.ArgumentParser(description="AI 代码评审双检助手")
    parser.add_argument("code", nargs="?", help="代码文件路径；为空时读标准输入")
    parser.add_argument("--code-string", dest="code_string", help="直接传入代码字符串")
    parser.add_argument("--lang", required=True, help="语言，如 python / go / javascript")
    parser.add_argument("--framework", default="", help="框架/库上下文")
    parser.add_argument("--config", default="", help="配置文件路径")
    parser.add_argument("--no-cache", action="store_true", help="跳过缓存")
    parser.add_argument("--json", action="store_true", help="输出 JSON")
    args = parser.parse_args()

    if args.code_string:
        src = args.code_string
    elif args.code:
        src = Path(args.code).read_text(encoding="utf-8")
    else:
        src = sys.stdin.read()

    report = review(src, args.lang, args.framework, args.config)
    if args.json:
        print(json.dumps(report.__dict__, ensure_ascii=False, indent=2))
    else:
        print(_fmt(report))


if __name__ == "__main__":
    # Smoke: 用坏代码跑一次，验证双检出报告
    bad = '''def f(a=[]):
    try:
        x = 1
    except:
        pass
'''
    r = review(bad, "python")
    assert r.risk in ("修复后合并", "阻止合并"), r.risk
    print("app smoke PASS:", r.summary)
```

- [ ] **Step 2: Run smoke check**

```bash
cd skill/scripts && python app.py
```

Expected: `app smoke PASS: ...`

- [ ] **Step 3: Commit**

```bash
git add skill/scripts/app.py
git commit -m "feat: add app orchestrator, merge, and CLI"
```

---

## Task 8: 创建测试样本文件

**Files:**
- Create: `data/sample_buggy_code.py`
- Create: `data/sample_clean_code.py`
- Create: `data/sample_go_code.go`

**Interfaces:**
- Produces: 供 v0.1 验证用的代码语料

- [ ] **Step 1: Write sample_buggy_code.py**

```python
# BUG: mutable default argument
def add_item(item, cache=[]):
    cache.append(item)
    return cache

# BUG: bare except
def read_file(path):
    try:
        f = open(path)
        return f.read()
    except:
        return ""

# BUG: deep nesting + is literal
def classify(value):
    if value is True:
        if value is not None:
            if value != False:
                if value == 1:
                    return "yes"
    return "no"

# BUG: TODO marker
# TODO: refactor this before release
```

- [ ] **Step 2: Write sample_clean_code.py**

```python
def add_item(item, cache=None):
    if cache is None:
        cache = []
    cache.append(item)
    return cache


def read_file(path):
    try:
        with open(path) as f:
            return f.read()
    except FileNotFoundError:
        return ""


def classify(value):
    if value is True:
        return "yes"
    return "no"
```

- [ ] **Step 3: Write sample_go_code.go**

```go
package main

import "fmt"

// BUG: ignored error
func read() {
    f, _ := os.Open("data.txt")
    defer f.Close()
    fmt.Println(f)
}

func main() {
    read()
}
```

- [ ] **Step 4: Commit**

```bash
git add data/
git commit -m "test: add python and go sample code for v0.1 baseline"
```

---

## Task 9: 端到端运行并记录回归基线

**Files:**
- Create: `tests/test_record.md`
- Create: `iteration/iteration_log.md`
- Modify: `README.md`

**Interfaces:**
- Consumes: 前面所有模块
- Produces: v0.1 迭代记录 + 回归基线

- [ ] **Step 1: Run static-only smoke (no AI keys)**

```bash
# Without API keys, the chain should degrade to static-only and still produce a report.
python skill/scripts/app.py data/sample_buggy_code.py --lang python --json > tests/buggy_output.json
python skill/scripts/app.py data/sample_clean_code.py --lang python --json > tests/clean_output.json
python skill/scripts/app.py data/sample_go_code.go --lang go --json > tests/go_output.json
```

Expected: three JSON files exist, each contain `risk`, `summary`, `findings`.

- [ ] **Step 2: Run with API keys (optional but recommended)**

If `DEEPSEEK_API_KEY` and/or `DASHSCOPE_API_KEY` are set:

```bash
python skill/scripts/app.py data/sample_buggy_code.py --lang python --framework stdlib --json > tests/buggy_ai_output.json
```

Expected: `findings` includes at least one `ai_new` or `ai_confirmed` item beyond static findings.

- [ ] **Step 3: Verify CQRS cache hit**

```bash
python skill/scripts/app.py data/sample_buggy_code.py --lang python --json > tests/buggy_cached.json
time python skill/scripts/app.py data/sample_buggy_code.py --lang python --json > /dev/null
```

Expected: second run completes in <100 ms (cache hit).

- [ ] **Step 4: Verify force re-review**

```bash
python skill/scripts/app.py data/sample_buggy_code.py --lang python --no-cache --json > tests/buggy_nocache.json
```

Expected: runs again without cache (ignore `--no-cache` in v0.1 if unimplemented; document in iteration log).

- [ ] **Step 5: Write tests/test_record.md**

Use the actual outputs from Steps 1-4. Template:

```markdown
# 测试记录

## v0.1 验证 (2026-07-11)

### 样本 1: sample_buggy_code.py（降级模式）
- 验证对象：静态快检 + AI 熔断降级本地兜底
- 命令：`python skill/scripts/app.py data/sample_buggy_code.py --lang python --json`
- 预期：返回 JSON，包含 static findings，AI 层标注降级
- 实际：<粘贴 tests/buggy_output.json 内容>
- 结论：通过 / 失败原因
- 后续：v0.2 用真实 AI 跑一遍，确认 AI 能发现额外问题

### 样本 2: sample_clean_code.py
- 验证对象：对照组，静态不误报严重问题
- 命令：`python skill/scripts/app.py data/sample_clean_code.py --lang python --json`
- 预期：risk 为 "可合并" 或 "修复后合并"
- 实际：<粘贴 tests/clean_output.json 内容>
- 结论：通过 / 失败原因
- 后续：v0.2 验证 AI 层不误报

### 样本 3: sample_go_code.go
- 验证对象：非 Python 语言路径，静态层正则不崩
- 命令：`python skill/scripts/app.py data/sample_go_code.go --lang go --json`
- 预期：返回 JSON，findings 至少包含 long_line/todo 类检查（正则层）
- 实际：<粘贴 tests/go_output.json 内容>
- 结论：通过 / 失败原因
- 后续：v0.3 扩展 Go 静态规则

### 缓存命中验证
- 命令：`time python skill/scripts/app.py data/sample_buggy_code.py --lang python --json`
- 预期：第二次运行显著快于第一次（<100ms）
- 实际：第一次 ___ ms，第二次 ___ ms
- 结论：通过 / 失败原因
```

- [ ] **Step 6: Write iteration/iteration_log.md**

```markdown
# 迭代日志

## 2026-07-11 20:00

- 更新类型：feature
- 版本迁移：v0.0 -> v0.1
- 更新摘要：落地 AI 代码评审「双检」MVP，实现静态快检 + AI 语义深检、CQRS 缓存、DeepSeek→Qwen 降级链、熔断器保护和状态机流程。
- 涉及文件：skill/scripts/app.py, circuit_breaker.py, cqrs_router.py, fallback_chain.py, state_machine.py, static_check.py；skill/references/config.example.json；data/sample_*.py, data/sample_go_code.go；tests/test_record.md
- 使用方式：
  - 静态/降级模式：`python skill/scripts/app.py data/sample_buggy_code.py --lang python`
  - 带 AI：`DEEPSEEK_API_KEY=xxx python skill/scripts/app.py data/sample_buggy_code.py --lang python --framework stdlib`
  - JSON 输出：加 `--json`
- 验证结果：<粘贴关键输出摘要，例如缓存命中耗时、降级模式输出>
- 后续注意：v0.2 将强化鲁棒性（熔断 cooldown、HALF_OPEN 探针、TTL 清理、更厚的本地兜底），需用真实 API key 跑 AI 路径。
```

- [ ] **Step 7: Write README.md**

```markdown
# AI 代码评审「双检」助手

> 静态规则快检 + AI 语义深检，让独立开发者也能拥有稳定可靠的代码 Review。

## 核心能力

- **双检**：静态层抓表面问题，AI 层解释告警并发现逻辑 Bug。
- **CQRS 缓存**：重复评审同段代码，第二次毫秒级返回。
- **模型降级链**：DeepSeek 失败自动切 Qwen，均失败则基于静态结果给出最小说明。
- **熔断器**：AI 子系统异常时退化为纯静态报告，不挂掉。

## 快速开始

```bash
# 1. 复制配置并填入 API key
export DEEPSEEK_API_KEY=your_key
# 可选：export DASHSCOPE_API_KEY=your_key

# 2. 评审一个文件
python skill/scripts/app.py data/sample_buggy_code.py --lang python

# 3. JSON 输出
python skill/scripts/app.py data/sample_buggy_code.py --lang python --json
```

## 项目结构

- `skill/scripts/`：六个核心脚本，每个 <200 行
- `skill/references/`：配置示例、使用说明、最佳实践
- `data/`：测试语料
- `tests/test_record.md`：回归基线
- `iteration/iteration_log.md`：迭代记录

## 迭代路线

- v0.1：双检并行 MVP（当前）
- v0.2：鲁棒性强化（熔断 cooldown、TTL 清理、更厚兜底）
- v0.3：多语言扩展 + 输出优化 + 沉淀 SKILL.md

详见 `iteration/iteration_log.md` 与 `docs/superpowers/specs/2026-07-11-dual-check-design.md`。
```

- [ ] **Step 8: Commit**

```bash
git add tests/ iteration/ README.md
git commit -m "docs: add v0.1 test baseline, iteration log, and README"
```

---

## Self-Review

### 1. Spec Coverage

| Spec 要求 | 对应任务 | 状态 |
|-----------|----------|------|
| 双检 = 静态 + AI | Task 2 + Task 5/7 | ✅ |
| 静态层 <200 行、ast + 正则 | Task 2 | ✅ |
| 熔断器在 AI 入口、fallback 链在闸门后 | Task 3 + Task 5 | ✅ |
| CQRS 全报告缓存 by hash(code+lang+model_ver) | Task 4 + Task 7 | ✅ |
| 本地兜底 v0.1 做实、基于静态结果 | Task 5 | ✅ |
| DeepSeek→Qwen 配置驱动 | Task 1 + Task 5 | ✅ |
| 状态机驱动流程 | Task 6 + Task 7 | ✅ |
| 记录文档而非 pytest | Task 9 | ✅ |
| 三轮迭代、每轮等指令 | Task 9（只覆盖 v0.1）| ✅ |
| ai_deep_check 归属 fallback_chain 调用 + app 解析 | Task 5 + Task 7 | ✅ |

**Gaps:**
- v0.2/v0.3 未在本计划中展开（用户要求每轮结束等指令，只写 v0.1）。

### 2. Placeholder Scan

- 无 `TBD` / `TODO` / `implement later` / `fill in details` / `similar to Task N`。
- 每步都有完整代码、命令、预期输出。
- `config.example.yaml` vs `.json` 不是占位符，是已标记的 spec 偏差。

### 3. Type Consistency

- `Finding`, `StaticReport` 在 `static_check.py` 定义，`fallback_chain.py` 和 `app.py` 引用 —— 一致。
- `AIReport`, `FinalReport` 在 `app.py` 定义，`merge` 使用 —— 一致。
- `CircuitBreaker.allow()` / `record_success()` / `record_failure()` 签名在 Task 3 与 Task 5 使用处一致。
- `next_state(state, event) -> tuple[str, str]` 在 Task 6 与 Task 7 使用处一致。
- `make_key(code, lang, model_ver) -> str` 在 Task 4 与 Task 7 使用处一致。

### 4. Known Deviations from Spec (Flagged for User Review)

1. **`config.example.yaml` → `config.example.json`**
   - Spec §4.1/§5 显示 YAML。
   - 偏差原因：硬约束「纯 Python 标准库」，PyYAML 非标准库。
   - 替代方案：保留 YAML 需引入约 40 行的 YAML 子集解析器或接受第三方依赖。
   - 影响：配置格式变化，功能语义完全等价。

2. **Spec §3 line 94 still says "每个文件 < 120 行"**
   - 用户最新指令已改为 <200 行。
   - 本计划所有模块预算均按 <200 行控制。

3. **`merge()` placed in `app.py`**
   - Spec 列出 `merge()` 为关键契约但未分配文件。
   - 原因：保持 6 文件约束，避免 `static_check.py` 反向导入 `app.py` 造成循环依赖。
   - 替代方案：新增 `report.py` 或 `merge.py` 作为第 7 个文件，需放宽 6 文件约束。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-11-v0.1-dual-check-mvp.md`.

**Two execution options:**

1. **Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration.

2. **Inline Execution** - Execute tasks in this session using `executing-plans`, batch execution with checkpoints.

Which approach?
