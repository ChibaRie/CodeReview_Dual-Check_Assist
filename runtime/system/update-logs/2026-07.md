# 增量日志 (system/update-logs/2026-07.md)

## 2026-07-15

- **更新类型**: feat
- **版本迁移**: v0.10.0 → v1.0.1
- **更新摘要**: 新增本地 Web UI 前端可视化（React 18 + Vite 5 + TypeScript + Tailwind CSS），FastAPI 后端 (`web_server.py`)，前端输入 DeepSeek API Key，后端代理调用。支持单文件评审（Monaco 编辑器）、批量评审、系统仪表盘。
- **涉及文件**:
  - 新增: `web_server.py`（FastAPI，7 端点），`web/`（React 前端 32 文件），根目录 `package.json`（concurrently）
  - 修改: `skill/scripts/fallback_chain.py`（`api_key` 参数），`skill/scripts/reviewer.py`（透传 `api_key`），`skill/SKILL.md`，`README.md`，`iteration/iteration_log.md`，`tests/test_record.md`，`system/status.md`，`runtime/AGENTS.md`
  - 新增测试: `tests/test_fallback_chain_api_key.py`(4)，`tests/test_reviewer_api_key.py`(4)，`tests/test_web_server.py`(42)
- **使用方式**:
  - Web UI：`npm run dev`（Vite :5173 + FastAPI :8000）
  - CLI 兼容：`python skill/scripts/app.py <文件> --lang python`（无退化）
  - API Key：前端 `localStorage` 输入，后端透传到 DeepSeek，不记录不持久化
- **验证结果**:
  - 218/218 pytest 全部通过
  - `npm run build` 构建成功（57 modules, ~193 KB JS）
  - `web_server.py` 覆盖率 99%
  - CLI `app.py --smoke` PASS
- **后续注意**: `skill/` 目录保持独立不受污染；`web_server.py` 与 `web/` 不属于 skill 分发包。

## 2026-07-12 01:00

- **更新类型**: feat
- **版本迁移**: v0.1 → v0.2
- **更新摘要**: 引入 blog_knowledge_sys 的文件格式与结构思维。新建 AGENTS.md、SYSTEM_SPEC.md、system/ 目录结构、声明式规则文件（python.yaml / universal.yaml）、frontmatter 标准化、distill/patterns/ 蒸馏卡片目录。
- **涉及文件**:
  - 新增: `AGENTS.md`, `SYSTEM_SPEC.md`, `system/review-rules.json`, `system/frontmatter-template.md`, `system/update-logs/README.md`, `system/logs/README.md`, `skill/references/rules/python.yaml`, `skill/references/rules/universal.yaml`, `distill/patterns/python/mutable-default-args.md`
  - 修改: `skill/scripts/static_check.py`（`_regex_check` 和 `static_check` 接受 `max_line_length` 参数）, `skill/scripts/app.py`（传递 `max_line_length`）, `skill/references/config.example.json`（增加 `max_line_length`）, `README.md`, `tests/test_record.md`, `iteration/iteration_log.md`（添加 YAML frontmatter）
- **使用方式**:
  - `max_line_length` 现可通过配置文件调整：修改 `config.example.json` 中 `review.max_line_length` 字段
  - 声明式规则文件位于 `skill/references/rules/`，作为人类可读的评审规则参考
  - 系统注册表 `system/review-rules.json` 描述了所有语言、规则和严重度
  - 蒸馏卡片 `distill/patterns/` 记录从实际 review 中提炼的 Bug 模式
- **验证结果**:
  - `static_check.py` smoke tests 通过
  - `circuit_breaker.py` smoke tests 通过
  - `app.py` smoke test 通过
  - E2E 基线：buggy=6, clean=0, go=2 — 与 v0.1 一致
- **后续注意**:
  - v0.2 剩余技术任务：熔断 cooldown/HALF_OPEN 探针/故障衰减、本地兜底升级、TTL 清理、健康度自检命令
  - go.yaml 和 javascript.yaml 规则文件作为 v0.3 扩展时的占位
  - 蒸馏卡片当前仅首张示例（mutable-default-args），后续应从实际 AI 评审结果中持续提取

## 2026-07-11 20:00

- **更新类型**: feat
- **版本迁移**: v0.0 → v0.1
- **更新摘要**: 落地 AI 代码评审「双检」MVP，实现静态快检 + AI 语义深检、CQRS 缓存、DeepSeek→Qwen 降级链、熔断器保护和状态机流程。
- **涉及文件**: `skill/scripts/app.py`, `circuit_breaker.py`, `cqrs_router.py`, `fallback_chain.py`, `state_machine.py`, `static_check.py`；`skill/references/config.example.json`；`data/sample_*.py`, `data/sample_go_code.go`；`tests/test_record.md`
- **使用方式**:
  - 静态/降级模式：`python skill/scripts/app.py <code-file> --lang python`
  - 带 AI：`DEEPSEEK_API_KEY=xxx python skill/scripts/app.py <code-file> --lang python --framework stdlib`
  - JSON 输出：加 `--json`
- **验证结果**:
  - `sample_buggy_code.py` 静态-only 返回 6 条 findings，risk=阻止合并
  - `sample_clean_code.py` 返回 0 条 findings，risk=可合并
  - `sample_go_code.go` 非 Python 路径正常返回 2 条正则层 findings，risk=修复后合并
  - 缓存命中：第二次运行约 114.5 ms（首次 159.2 ms），cache 有效
  - `--no-cache` 可强制重新评审
- **后续注意**: v0.2 将强化鲁棒性（熔断 cooldown、HALF_OPEN 探针、TTL 清理、更厚的本地兜底），需用真实 API key 跑 AI 路径。
