---
date: 2026-07-12T00:00:00+08:00
type: document
topic: code-review
tags: []
source:
status: draft
---

> `type` 使用 `review-record`（评审记录）、`iteration-log`（迭代日志）、`design`（设计文档）、`document`（说明文档）。
> `topic` 使用 `code-review`、`static-check`、`ai-check`、`cache`、`breaker`、`merge`、`system`。
> 修改或新增文档后，确保内容与 `tests/test_record.md` 的基线一致。

# 标题

## 背景

为什么记录这个问题。

## 核心内容

## 可复用点

## 后续问题
